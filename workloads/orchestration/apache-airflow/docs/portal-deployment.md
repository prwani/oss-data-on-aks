# Apache Airflow portal deployment path

The portal workflow should validate:

- metadata service dependencies
- executor and worker scaling approach
- ingress and authentication path for the UI
- DAG execution validation after deployment

