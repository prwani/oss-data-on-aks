# Apache Airflow on AKS

This blueprint is the starter path for running **Apache Airflow** on AKS with AKS AVM as the cluster baseline.

## Intended scope

- shared AKS baseline
- Airflow-specific deployment choices for webserver, scheduler, and workers
- dependency guidance for metadata database and executor model
- portal and `az` CLI operator flows

