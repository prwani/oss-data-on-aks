# Redpanda on AKS

This blueprint is the starter path for running **Redpanda** on AKS with an AKS AVM cluster baseline.

## Intended scope

- shared AKS deployment baseline
- workload-specific Terraform and Bicep entry points
- operator guidance for stateful streaming clusters
- portal and `az` CLI delivery paths

