# OpenSearch CLI scripts

This folder is reserved for helper scripts that should stay close to the OpenSearch blueprint.

As the blueprint matures, prefer scripts that focus on:

- resource group and prerequisite validation
- AKS credential acquisition
- namespace bootstrap
- install and validation helpers
