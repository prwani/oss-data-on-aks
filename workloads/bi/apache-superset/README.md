# Apache Superset on AKS

This blueprint is the starter path for running **Apache Superset** on AKS with AKS AVM as the cluster baseline.

## Intended scope

- shared AKS baseline
- Superset-specific deployment and access guidance
- secret, metadata, and connectivity considerations
- portal and `az` CLI operator flows

