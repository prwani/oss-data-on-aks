# Apache Superset `az` CLI deployment path

Recommended automation flow:

1. deploy the shared AKS baseline
2. provision secrets and metadata dependencies
3. install Superset components
4. validate sign-in, metadata persistence, and query connectivity

