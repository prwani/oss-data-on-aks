# Script helper template

Use this folder only for small helper wrappers that stay aligned with the checked-in deployment docs.

## Expected structure

- `az-cli/` for automation-first helper notes or scripts

The authoritative install flow should remain in `docs/az-cli-deployment.md`. Add scripts here only when they make a documented path easier to rerun without hiding important decisions.
